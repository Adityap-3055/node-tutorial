// in this module we are exporting two things,
// one is the array(items) and the other is a 
// person object.
module.exports.items = ['item1', 'item2'];

const person = {
    name: 'bob'
}

module.exports.singlePerson = person;